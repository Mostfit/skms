.top-form
  = partial('status')

.updates
  - for tweet in @tweets
    .tweet
      .user
        .image
          = image_tag tweet.user.image.url(:thumb), :height=>'60px'
        .nick
          -if tweet.user.nick
            = tweet.user.nick
      .content
        %p.content
          = tweet.content
        .view
          = link_to 'View', url(:edit_tweet,  tweet)
      .tags
        -tweet.tags.each do |tag|
          =tag.name
      .extra
        == #{tweet.comments.count} Comments
        - if tweet.file?
          == Attachments
      %hr
