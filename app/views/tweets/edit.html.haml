.tweet
  .user
    = image_tag @tweet.user.image.url(:thumb), :height=>'50px'
    %span.user-nick
      -if @tweet.user.nick
        = @tweet.user.nick
  .content
    %p.content
      = @tweet.content
.comment
  %h3 Comments
  = render :template => 'comments/index', :layout => false
.attachment
  %h3 Attachments
  = partial 'attachment'
.tags
  %h3 Tags
  = render :template => 'tags/index', :layout => false
