%h1
  Events controller, edit action
%p
  Edit this file in
  %tt
    app/views/events/edit.html.erb
= error_messages_for @event

= form_for(@event, :action => url(:event, @event)) do
  %p
    = submit "Update"

= link_to 'Show', url(:event, @event)
|
= link_to 'Back', url(:events)