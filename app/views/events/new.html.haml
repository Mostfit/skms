%h1 Events controller, new action

%p 
  Edit this file in 
  %tt app/views/events/new.html.erb

= error_messages_for @event

= form_for(@event, :action => url(:events) ) do
  %p
    = submit "Create"

= link_to 'Back', url(:events)